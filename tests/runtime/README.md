# Tests runtime

Dossier transversal volontaire.

Il teste le cycle de vie global de `ClavigerBot`, la composition, les événements Discord multi-guild et les interactions entre plusieurs couches.

Il constitue une exception documentée au miroir strict `src ↔ tests`, car ces scénarios portent sur l'application assemblée plutôt que sur un module unique.
