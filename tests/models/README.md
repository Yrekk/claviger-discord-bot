# Tests des models

Tests des modèles lorsque ceux-ci portent une sémantique, une propriété calculée ou un invariant qui mérite d'être vérifié.

Un simple conteneur de données ne nécessite pas forcément un test dédié.
